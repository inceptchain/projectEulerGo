package main 

import (
	"fmt"
)

func main() {
	//track coordinates
	//run through each combination, removing duplicate combinations.
	for x := 0; x<20; x++ {
		for y := 0; y<20; y++ {

		}
	} 
			
	
}